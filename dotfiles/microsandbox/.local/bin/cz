#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"

exec msbe \
  -e CZ_FILTER_PATH \
  -v "$REPO_ROOT:$REPO_ROOT" \
  -w "$PWD" \
  ghcr.io/shotor/commitizen:latest \
  cz "$@"
